extends layout

block content	
	- for(var i = 0; i < added.length; i++)
		mixin post(added[i].name, added[i].text, added[i].date, added[i].comment, '/post/' + added[i].id, added[i].rubric)
	- if(extra[0] != 0)
		a(href=extra2 + (+extra[0] - 1))
			article.page_control.but_read.page_control_prev Назад
	- if(extra[0] != extra[1])
		a(href=extra2 + (+extra[0] + 1))
			article.page_control.but_read.page_control_next Вперёд
//- block panel
//- 	article.pan
//- 		article.pan_title Главное
//- 		article.pan_list Первый
//- 		article.pan_list Второй
//- 		article.pan_list Третий
//- 	article.pan
//- 		article.pan_title Главное
//- 		article.pan_list Первый
//- 		article.pan_list Второй
//- 		article.pan_list Третий
//- 	article.pan
//- 		article.pan_title Главное
//- 		article.pan_list Первый
//- 		article.pan_list Второй
//- 		article.pan_list Третий